<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp"
    android:gravity="center"
    android:orientation="vertical">

    <TextView
        android:id="@+id/bannerTitleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp"
        android:textSize="16sp"
        android:textStyle="bold"
        android:gravity="center"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:background="#1565c0"
        android:text="New Surveys are available for you!"
        tools:textColor="#ffffff" />

    <ImageButton
        android:id="@+id/closeBannerButton"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:background="@drawable/cpx_close_button_background"
        android:src="@drawable/cpx_close"
        app:layout_constraintBottom_toTopOf="@id/bannerTitleTextView"
        app:layout_constraintEnd_toEndOf="@id/bannerTitleTextView"
        app:layout_constraintStart_toEndOf="@id/bannerTitleTextView"
        app:layout_constraintTop_toTopOf="@id/bannerTitleTextView" />

</androidx.constraintlayout.widget.ConstraintLayout>
